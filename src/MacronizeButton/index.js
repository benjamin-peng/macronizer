const MacronizeButton = ({macronize}) => {
    return (  
        <div className="MacronizeButton">
            <button onClick={macronize}>macronize</button>
        </div>
    );
}
 
export default MacronizeButton;